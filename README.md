# Personal Portfolio

## Installation

This project is built with React, Vite, and Tailwind CSS. Install the JavaScript dependencies using npm:

```bash
npm install
```

Then start the development server:

```bash
npm run dev
```
